 piperg2p/spans.py         |  3 +++
 piperg2p/types.py         |  1 +
 tests/test_annotations.py | 36 ++++++++++++++++++++++++++++++++++--
 3 files changed, 38 insertions(+), 2 deletions(-)

diff --git a/piperg2p/spans.py b/piperg2p/spans.py
index c694a7d..3574ef2 100644
--- a/piperg2p/spans.py
+++ b/piperg2p/spans.py
@@ -54,6 +54,7 @@ def _as_annotation(value: TokenAnnotationLike) -> TokenAnnotation:
             value.get("tag"),
             value.get("lemma"),
             value.get("language"),
+            value.get("morph"),
         )
     raise TypeError("annotation must be TokenAnnotation or mapping")
 
@@ -157,6 +158,8 @@ def _update_token_metadata(
                     token.meta["tag"] = annotation.tag
                 if annotation.lemma is not None:
                     token.meta["lemma"] = annotation.lemma
+                if annotation.morph is not None:
+                    token.meta["morph"] = annotation.morph
                 if annotation.language is not None:
                     token.lang = annotation.language
     for override in overrides:
diff --git a/piperg2p/types.py b/piperg2p/types.py
index 5d8b482..b5637bd 100644
--- a/piperg2p/types.py
+++ b/piperg2p/types.py
@@ -59,6 +59,7 @@ class TokenAnnotation:
     tag: str | None = None
     lemma: str | None = None
     language: str | None = None
+    morph: str | None = None
 
     @property
     def char_start(self) -> int:
diff --git a/tests/test_annotations.py b/tests/test_annotations.py
index 4ac59cb..41e720c 100644
--- a/tests/test_annotations.py
+++ b/tests/test_annotations.py
@@ -15,8 +15,40 @@ def test_annotations_are_source_aligned_and_preserved_in_token_metadata():
     result = g2p.phonemize_prepared(
         "ab c",
         annotations=[
-            TokenAnnotation(0, 2, text="ab", pos="NOUN", tag="NN", lemma="ab")
+            TokenAnnotation(
+                0,
+                2,
+                text="ab",
+                pos="NOUN",
+                tag="NN",
+                lemma="ab",
+                morph="Number=Sing",
+            )
         ],
     )
-    assert result.tokens[0].meta == {"pos": "NOUN", "tag": "NN", "lemma": "ab"}
+    assert result.tokens[0].meta == {
+        "pos": "NOUN",
+        "tag": "NN",
+        "lemma": "ab",
+        "morph": "Number=Sing",
+    }
+    g2p.close()
+
+
+def test_annotation_mapping_preserves_morph_metadata():
+    config = VoiceConfig.from_dict(
+        {
+            "num_symbols": 4,
+            "num_speakers": 1,
+            "audio": {"sample_rate": 22050},
+            "phoneme_type": "text",
+            "phoneme_id_map": {"_": 0, "^": 1, "$": 2, "a": 3},
+        }
+    )
+    g2p = PiperG2P("en-us", config)
+    result = g2p.phonemize_prepared(
+        "a",
+        annotations=[{"start": 0, "end": 1, "text": "a", "morph": "VerbForm=Fin"}],
+    )
+    assert result.tokens[0].meta == {"morph": "VerbForm=Fin"}
     g2p.close()
