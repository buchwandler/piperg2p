# Diagnostics API

`BackendDiagnostics` and `FrontendDiagnostics` are frozen dataclasses. Backend diagnostics include implementation, discovered paths, version, clause API availability, fallback reason, warnings, and parity.
